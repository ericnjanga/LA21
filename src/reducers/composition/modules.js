/**
 * Manage modules in the application
 * (Modules are groups of test questions of a specific nature)
 */
import actionTypes from './../../actions/_actionTypes'

const modules = (stateModules = [], action) => {
  switch(action.type) {
    default:
      return stateModules
  }
}

export default modules
