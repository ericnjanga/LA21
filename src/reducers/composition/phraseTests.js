/**
 * Manage phraseTests in the application
 * ("phraase tests" are groups of test questions whith contains "phrases" and require answers)
 */
import actionTypes from './../../actions/_actionTypes'

const phraseTests = (statePhraseTests = [], action) => {
  switch(action.type) {
    default:
      return statePhraseTests
  }
}

export default phraseTests
